<UserControl x:Class="RegistryExpert.Wpf.Views.DiskBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="800">
    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1"
            Padding="0">
        <Grid x:Name="RootGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />     <!-- Disk header -->
                <ColumnDefinition Width="*" />       <!-- Partition strip -->
            </Grid.ColumnDefinitions>

            <!-- Disk header (left, fixed width) -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0"
                    Padding="8,6">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock x:Name="DiskNumberText"
                               Text="Disk 0"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Foreground="{DynamicResource TextPrimaryBrush}" />
                    <TextBlock x:Name="DiskTypeText"
                               Text="Basic"
                               FontSize="10"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock x:Name="DiskCapacityText"
                               Text=""
                               FontSize="10"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock x:Name="DiskStatusText"
                               Text="Online"
                               FontSize="10"
                               Margin="0,2,0,0"
                               Foreground="{DynamicResource AccentBrush}" />
                </StackPanel>
            </Border>

            <!-- Partition strip (right, proportional) -->
            <Grid x:Name="PartitionStrip" Grid.Column="1" Background="Transparent" />
        </Grid>
    </Border>
</UserControl>
